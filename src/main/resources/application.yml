server:
  port: 80
  address: 0.0.0.0

spring:
  application:
    name: dockerTestApp
  datasource:
    url: jdbc:postgresql://35.190.236.211:5432/webatoz
    username: admin
    password: cafe24@001
    driver-class-name: org.postgresql.Driver

ribbon:
  ServerListRefreshInterval: 1000

endpoints:
  health:
    sensitive: false
  restart:
    enabled: true
  shutdown:
    enabled: true
